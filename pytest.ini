[pytest]
asyncio_mode = auto
testpaths = tests
# Backstop: no single test should run this long. A hang fails fast instead of
# wedging CI for its full 6h ceiling (see tests/test_sse.py for the history).
timeout = 60
timeout_method = thread
